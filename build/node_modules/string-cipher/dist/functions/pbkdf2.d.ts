/// <reference types="node" />
import { BinaryLike } from 'crypto';
export declare const pbkdf2: (password: BinaryLike, salt: BinaryLike, iterations: number, keyLength: number, digest: string) => Promise<Buffer>;
